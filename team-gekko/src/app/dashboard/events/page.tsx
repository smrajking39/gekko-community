import { DashboardPlaceholder } from '@/components/dashboard/dashboard-placeholder';

export const metadata = { title: 'Events' };

export default function DashboardEventsPage() {
  return <DashboardPlaceholder title="Events" />;
}
